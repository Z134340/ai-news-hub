node_modules/
.DS_Store
.claude/
__pycache__/
*.pyc
data/logs/*.log
data/logs/validate-*.json
data/logs/failed_*.txt
*.tmp
tmp_*

# repo 瘦身：不追蹤本機 git bundle 備份與後端中繼產物
ai-news-hub.bundle
data/emerging.json

# Firebase writer 憑證（冷封存用）絕不進 repo
*.env
.archiver.env
archiver.env

# 維運手冊：repo 為 public，ops 細節僅留本機（如需備份請存私有知識庫）
OPS-RUNBOOK.md

# agent 輸出的預覽區：build-insights.mjs 預設產到這裡供人審，核可後才用 --promote
# 落到 data/agent/。run-daily.sh 每天會 `git add data/`，沒有這條規則預覽檔會直接上線。
data/agent/.preview/
