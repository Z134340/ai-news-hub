<!-- 自 CLAUDE.md 拆出（2026-09-04）。此檔是權威規範，CLAUDE.md 只留索引；改本檔不必同步回 CLAUDE.md。 -->

### G. `scripts/agent/` 其他腳本（一行一檔）

| 檔 | 用途 / 旗標 / 輸出 |
|---|---|
| `build-insights.mjs` | 語料 → `trends.json`、`candidates.json`、`recommendations.json`；`--window --out-dir --promote --self-test` |
| `build-timeline.mjs` | `lib/trend-metrics.mjs` 每日頻次 → `timeline.json`；同上旗標 |
| `build-roadmap-input.mjs` | timeline + trend-assessment → `roadmap-input.json`；`--timeline --trend --curation --clusters --out --promote --self-test` |
| `build-brief-input.mjs` | 語料 + timeline + 兩份上游 → `brief-input.json`（`brief-input-v0.1`） |
| `harvest-precedents.mjs` | 閘 1 降級紀錄 → 判例候選（`.preview/precedent-proposals/`） |
| `build-current-state-manifest.mjs` / `build-system-status.mjs` | observer 09/10；`--out --root --self-test`、`--manifest --out --root --schema --self-test` |
| `validate-system-status-schema.mjs`、`verify-dashboard-system-status.mjs`、`freshness-release-gate.mjs`、`build-review-packet.mjs` | 驗證 / 一致性 / 新鮮度閘 / 人審包；皆有 `--self-test`（verify-dashboard 無旗標） |
| `promote.sh` | `.preview/` → `data/agent/` 人工晉升；`--apply --allow-degraded --window --root --self-test`；第 53 行 `NEVER_FILES="roadmap-input.json brief-input.json brief-input-7d.json agent-run-status.json current-state-manifest.json search-review-input.json change-eval-input.json weekly-report.json weekly-report.md"`（S3-E 起含週報兩檔）；P-2 只掃 `.preview/*.json` 頂層，子目錄不算 |
| `build-search-review-input.mjs` | Phase 2-5：metrics-history + 10 支 prompt marker 區段 + `PRIORITY_KEYWORDS` + human_ratings 聚合 + canaries 快照 → `.preview/search-review-input.json`（`search-review-input-v0.1`；在 NEVER_FILES）；`--window --metrics --out --self-test`（T-1..T-13；T-3 以 `!= null` 分辨「缺 marker」與「空區段」） |
| `build-change-eval-input.mjs` | Phase 3-A 閘 2 輸入。讀 `.preview/search-review.json`（只留 `status=pending_review`）＋ `proposals.json`（近 7 天 `evaluated/canary` 算 in_flight）＋ `agents/_control/canaries.json`（配額唯一來源，缺檔 → remaining 0）＋ metrics 14 晚 → `.preview/change-eval-input.json`（schema `change-eval-input-v0.1`，鍵 `source/quota/canaries_in_flight/proposals/metrics_window/allowlist_targets/boundary`）。上游無 `rollback` 時依 change_type 補並標 `rollback_source: derived`。`slimProposal` 保留 `patch`（非物件 → `null`；patch 內字串一樣過 `scrub`，含 URL 者整串換成 `[url-removed]`；S3-C2）。`quota.in_flight/in_flight_by_category/remaining` 只算 `production_applied===true` 的 in_flight（與 apply-change `countInFlight` 同尺），`quota.dedupe_in_flight` 與 `canaries_in_flight[]`（每筆帶 `production_applied`）仍含全部 7 天內 `evaluated/canary` 供閘 2 reversibility-check 去重。缺 search-review.json 仍寫出 `proposals: []`。`--self-test` T-1..T-12；export `build/scrub/deriveRollback/loadInFlight`。promote.sh NEVER_FILES 已含此檔。 |
| `build-weekly-report.mjs` | Phase 3-E（3-9）週報。讀 `.preview/precedent-proposals/prec-*.json`（依 `created_at` 取近 7 天；每則只留 `pid`=`[P-nnn]`、`proposal_id`、`agent`、`category`（檔內沒有則 `null`）、`status`、`situation` ≤120 字、`discriminator`）＋ `proposals.json`（近 7 天 `evaluated/canary/auto_applied/reverted` 計數與 `diff_summary` ≤160 字，時間欄 `evaluated_at/canary_started_at/confirmed_at/reverted_at`，缺則 `created_at`）＋ `metrics-history.jsonl`（`loadMetrics` 近 7 晚各分類 `verified_rate` 均值 3 位小數）→ `.preview/weekly-report.json`（schema `weekly-report-v0.1`，鍵 `generated_at/report_date/window_days/window_from/sources/precedent_count/precedents/pid_map/proposal_changes/metrics/note`）＋ `.preview/weekly-report.md`（Slack mrkdwn，判例一行一則、固定 `[P-nnn]` 開頭，供 3-11 對照 `pid_map`）。所有字串過 `scrub`（URL → `[url-removed]`），`assertNoLeak` 擋 `title/url/link/score/rating` 鍵與 `https?://`。`--self-test`（W-1..W-11）`--root --window --now --dry-run`；export `build/renderMd/run/scrub/assertNoLeak/loadPrecedents/loadProposalChanges/loadMetricsSummary`。兩檔皆在 promote.sh NEVER_FILES。 |
| `slack-notify.sh` | Phase 3-E（3-10）。`set -euo pipefail`；只用 grep 解析 `~/.config/ai-news-hub/slack.env` 的 `SLACK_BOT_TOKEN`／`SLACK_CHANNEL_ID`（不 `source`；缺檔或缺變數 → log「Slack 未設定，跳過」exit 0；週報缺檔也 exit 0）。`curl -sS -X POST $SLACK_API_BASE/chat.postMessage`，token 只走 `-H "Authorization: Bearer …"`，payload 由 python 產 `{channel,text(≤39000 字),mrkdwn}` 暫存檔；回應只印 `ok/ts/error`，成功追加 `{ts,channel,sent_at,report_date}` 到 `~/.ai-news-hub/learning/weekly-report-sent.jsonl`，`ok:false` → exit 1 不寫。旗標 `--dry-run --self-test --report FILE --json FILE`；環境變數 `SLACK_ENV_FILE/SLACK_API_BASE/SLACK_SENT_LOG/SLACK_NOTIFY_DRY_RUN=1`（run-agents `--dry-run` 會 export 後者）。`--self-test` N-1..N-7 用 python 起本機假 Slack，斷言 token 不進 stdout/stderr/jsonl/payload。run-agents 接線：`export AGENT_SCRIPTS`＋`run_step "08f-weekly-report" bash -c '[[ $(date +%u) -eq 7 ]] && node … && bash … \|\| true'`（08e 之後、09 observer 之前），self-test S-1／S-6q／S-6r。 |
| `read-slack-picks.mjs` | Phase 3-F（3-11）。讀 `~/.ai-news-hub/learning/weekly-report-sent.jsonl` 最後一筆有效 `{ts,channel}`，用 `GET $SLACK_API_BASE/reactions.get?channel&timestamp&full=true` 與 `conversations.replies?channel&ts&limit=200`（token 只走 `Authorization: Bearer`）。規則：本體 reaction 含 `white_check_mark`／`heavy_check_mark`／`ballot_box_with_check`（含 `::skin-tone` 變體）→ `picked_all:true`；討論串回覆（跳過 ts 等於本體的第一筆）用 `/\[?\bP-(\d{3})\b\]?/g` 擷取 → `picked` 去重排序。輸出 `.preview/precedent-picks.json`（`{schema:"precedent-picks/v1",report_ts,report_date,picked,picked_all,reply_count,reply_authors,fetched_at,note}`，覆寫）＋ append `~/.ai-news-hub/learning/precedent-picks.jsonl`（同物件一行）；回覆原文與作者 id 不落地，`assertNoToken` 擋 token。跳過條件全部 exit 0 零寫入：缺 slack.env、缺 `SLACK_BOT_TOKEN`、無 sent log、API 丟錯或 `ok:false`。不寫 memory/、不改 precedents.jsonl、不記帳本、不 import ledger。旗標 `--dry-run --self-test`；環境變數 `SLACK_ENV_FILE/SLACK_API_BASE/SLACK_SENT_LOG/AGENT_PREVIEW_DIR/AGENT_LEARNING_DIR`（自測用 tmp root＋注入 fetch，不起 server）。`export run()` 供自測呼叫（`{env,log,fetch,dryRun}`），只在直接執行時跑 `main()`。run-agents 接線：`00d-slack-picks`（00c 之後、01 之前，前後 `FAILED_STEP=""`；dry-run 帶 `PICKS_EXTRA=(--dry-run)`），self-test S-1／S-6s（21 條）。 |
| `lib/corpus.mjs` / `lib/lexicons.mjs` / `lib/trend-metrics.mjs` | 語料讀取（熱層 7 天、`~/.ai-news-hub/corpus/` 34 天）／六個固定 `cluster_id` 詞庫／決定論指標（刻意不消毒標題） |

